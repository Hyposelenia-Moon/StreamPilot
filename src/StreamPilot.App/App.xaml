<Application x:Class="StreamPilot.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <ResourceDictionary>
      <!-- 深色主题基础色板（UI 层只负责展示，业务逻辑在 ViewModel 与 Core 服务中） -->
      <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#101216" />
      <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#16191F" />
      <SolidColorBrush x:Key="BorderBrushSoft" Color="#262B33" />
      <SolidColorBrush x:Key="TextBrush" Color="#E6E9EE" />
      <SolidColorBrush x:Key="MutedTextBrush" Color="#98A2B3" />
      <SolidColorBrush x:Key="AccentBrush" Color="#4C8DFF" />
      <SolidColorBrush x:Key="WarnBrush" Color="#F0B429" />
      <SolidColorBrush x:Key="ErrorBrush" Color="#EF5350" />
      <SolidColorBrush x:Key="OkBrush" Color="#3DDC84" />

      <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>

      <Style x:Key="MutedText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>

      <Style TargetType="Button">
        <Setter Property="Background" Value="#1E232B" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,4" />
        <Setter Property="Margin" Value="0,0,6,0" />
        <Setter Property="MinWidth" Value="72" />
      </Style>

      <Style TargetType="TextBox">
        <Setter Property="Background" Value="#1E232B" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}" />
        <Setter Property="Padding" Value="6,4" />
      </Style>

      <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#1E232B" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="Padding" Value="6,3" />
      </Style>

      <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
