# StreamPilot 项目规范

## 项目概述

StreamPilot 是一个 Windows 桌面直播播放器，集成多平台低延迟播放、直播录制与回放功能。核心设计理念：Web 端负责低延迟观看，mpv 端负责超分/HDR/高画质，两者互不关联。

本项目为自研源码，最终以自包含单文件 exe 发布，用户解压后双击即可运行，无需安装任何环境。

## 通用约束

以下约束适用于所有语言和模块，优先级高于任何单一技术选型。

### 代码质量
- **禁止**提交带 `TODO`、`FIXME`、`HACK` 注释的代码到主分支
- **禁止**提交注释掉的代码块，需要时从 Git 历史恢复
- **禁止**提交超过 300 行的单个函数或方法
- **禁止**提交超过 5 层嵌套的代码块，超过就抽取
- **禁止**使用魔法数字，所有常量必须命名并集中定义
- **禁止**在生产代码中使用 `Console.WriteLine` / `print` / `echo` 调试输出
- 所有 public API 必须有文档注释（C# 用 XML，JS 用 JSDoc）
- 所有外部输入必须校验，禁止信任任何来自网络、文件、环境变量的数据

### 命名规范
- **C#**：类名 PascalCase，方法 PascalCase，私有字段 `_camelCase`，接口 `I` 前缀
- **JavaScript**：变量 camelCase，类 PascalCase，常量 UPPER_SNAKE_CASE
- **文件**：C# 用 PascalCase（`BilibiliParser.cs`），JS 用 kebab-case（`mpv-bridge.js`）
- **Git 分支**：`feat/xxx`、`fix/xxx`、`chore/xxx`，禁止中文分支名

### 架构边界
- **禁止**跨层调用：UI 层不得直接调用解析层，必须经过 Core 层的服务接口
- **禁止**循环依赖：`Core` 不依赖 `App`，`Bridge` 不依赖 `App`
- **禁止**在 UI 层写业务逻辑，UI 只负责展示和事件转发
- **禁止**在 Core 层引入 UI 框架依赖（WPF、WinForms、WinUI）
- 所有平台解析器必须实现统一的 `IPlatformParser` 接口
- 所有平台解析结果必须统一为 `StreamCandidate` 结构

### 错误处理
- **禁止**吞掉异常：所有 `catch` 块必须记录日志或重新抛出
- **禁止**裸 `catch (Exception) {}` 空实现
- 所有异步操作必须有超时保护，禁止无限等待
- 所有网络请求必须处理 `Timeout`、`ConnectionRefused`、`HTTP 4xx/5xx`
- 所有进程调用必须处理 `Process.Start` 失败、非零退出码、超时
- 错误信息必须包含上下文（模块名、操作、关键参数），禁止只输出"发生错误"

### 日志规范
- 使用结构化日志，禁止字符串拼接
- 日志级别：`Trace`（开发调试）、`Debug`（开发）、`Info`（关键流程）、`Warn`（可恢复异常）、`Error`（需要人工介入）
- 敏感信息（Cookie、Token、签名 URL）**禁止**写入日志
- 日志必须包含时间戳、级别、模块名、消息

### 安全规则
- **NEVER** 提交 `huya-cookie.txt`、`mpv-config.txt`、`.env` 等含隐私的文件到 Git
- **NEVER** 在前端代码中硬编码 Cookie、密钥、签名
- **NEVER** 绕过平台的验证码、登录、风控机制
- **NEVER** 将桥接服务端口暴露到公网，只监听 `127.0.0.1`
- **ALWAYS** 桥接服务响应头包含 `Access-Control-Allow-Private-Network: true`
- **ALWAYS** 解析出的流地址在使用前检查有效期
- **ALWAYS** 用户数据（Cookie、配置）与程序目录分离，方便清理

### 性能规则
- **禁止**在 UI 线程执行任何阻塞操作（网络请求、文件 IO、进程等待）
- **禁止**在主线程循环中做重计算，必须异步或后台线程
- 前端 `enableWorker` 必须为 `true`，避免解封装阻塞 UI
- 所有网络请求必须有重试上限（最多 3 次），禁止无限重试
- 所有超时必须显式指定，禁止依赖默认值
- 内存中缓存的流地址数量必须有上限，超出后淘汰最旧的

### 依赖管理
- **禁止**引入未经评估的第三方依赖，必须先在 ADR 中说明理由
- **禁止**引入体积超过 50MB 的依赖，除非有明确收益
- 所有第三方依赖必须在 `THIRD-PARTY-NOTICES.md` 中登记版本、许可证、来源
- 所有二进制依赖（ffmpeg、mpv）必须记录版本号和 SHA256

### 测试要求
- 所有解析逻辑必须有单元测试，覆盖正常、异常、边界三种情况
- 所有 HTTP 端点必须有集成测试
- 前端播放逻辑的回归测试使用 `node --test`
- **禁止**提交覆盖率低于 60% 的核心模块
- **禁止**用 `Assert.True(true)` 之类的空测试凑数

### Git 规范
- 提交信息使用 Conventional Commits 格式：`<type>: <description>`
- 类型：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`chore`、`build`、`ci`
- 提交前确认变更列表不包含 `*.dll`、`*.exe`、`publish/`、`mpv-config.txt`、`huya-cookie.txt`
- 每次提交只做一件事，禁止把多个不相关的改动打包提交
- 提交信息必须说明"为什么"，而不是"做了什么"

### 文档要求
- 所有公共 API 必须有使用示例
- 所有平台解析器必须在 `docs/parsers/` 下有对应的说明文档
- 所有 ADR 必须放在 `docs/adr/`，文件名格式 `NNNN-标题.md`
- README 必须说明：项目定位、安装、使用、平台支持、已知限制

## 禁止事项（红线）

以下行为在任何情况下都不允许：

1. **禁止**上传含隐私的文件（Cookie、Token、密钥、个人路径）
2. **禁止**上传官方二进制（`*.dll`、`*.exe`）到仓库
3. **禁止**绕过平台风控、验证码、登录
4. **禁止**在代码中硬编码任何密钥或凭证
5. **禁止**在 UI 线程执行阻塞操作
6. **禁止**吞掉异常不做处理
7. **禁止**无限重试或无限等待
8. **禁止**将桥接端口暴露到公网
9. **禁止**在未写 ADR 的情况下更换技术栈
10. **禁止**提交覆盖率低于 60% 的核心模块